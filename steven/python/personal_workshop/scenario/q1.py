#!/usr/bin/python3

# Print current directory

# os is a module that gives us more functions to use!
# When using modules, the syntax is typically <module_name>.<function>
import os

# Get current directory
current_directory = os.getcwd()
print(current_directory)
